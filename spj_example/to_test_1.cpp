#include <bits/stdc++.h>
using namespace std;
long long a, b;
// input two numbers
// print two numbers that has a sum of a + b
int main(int argc, char const *argv[]) {
    scanf("%lld%lld", &a, &b);
    printf("%lld %lld\n", a, b);
    return 0;
}
